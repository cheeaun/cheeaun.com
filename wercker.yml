box: node

build:
  steps:
    - npm-install
    - script:
        name: npm run build
        code: npm run build

deploy:
  steps:
    - lukevivier/gh-pages:
        token: $GH_TOKEN
        basedir: build
        domain: cheeaun.com
