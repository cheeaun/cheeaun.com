box: node

build:
  steps:
    - script:
      name: install dependencies
      code: npm i --production
    - script:
      name: build
      code: npm run build

deploy:
  steps:
    - lukevivier/gh-pages:
        token: $GH_TOKEN
        basedir: build
        domain: cheeaun.com
